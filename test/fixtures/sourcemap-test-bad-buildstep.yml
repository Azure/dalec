# syntax=ghcr.io/azure/dalec/frontend:latest

name: sourcemap-test-buildstep
description: Test spec with intentionally bad build step to verify source maps
website: https://example.com
version: 1.0.0
revision: 1
vendor: Test
packager: Test Packager
license: MIT

x-steps: &extra_steps
  - command: |
      echo "Starting build..."
      echo "Build step 1 completed"
  - command: |
      echo "This step will fail intentionally"
      exit 42
      echo "This should not be reached"




# This spec is designed to fail during the build step
# to test that source maps correctly point to the failing step
build:
  steps: *extra_steps
