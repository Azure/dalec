# syntax=ghcr.io/azure/dalec/frontend:latest

packager: "Azure container upstream"
description: "test package"
website: "microsoft.com"
version: "1.0"
revision: 1
license: Apache 2.0
name: "pinned-build-deps"

dependencies:
  build:
    curl:
      version: ["> 7.84", "< 7.86"]

build:
  steps:
  # newest version in mariner repos is 5.9
    - command: |
        [[ $(curl --version | head -n1 | awk '{{ print $2 }}') =~ 7\.84\.* ]]